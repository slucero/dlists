<p id="notice"><%= notice %></p>

<h1><%= @list.title %></h1>
<ul class="actions">
  <li><%= link_to 'Edit', edit_list_path(@list) %></li>
  <li><%= link_to 'Back', lists_path %></li>
</ul>

<p>
  <%= @list.summary %>
</p>

<div id="list-items">
  <h2>Items</h2>
  <%= render @items %>
</div>

<% #render 'items/form' %>
